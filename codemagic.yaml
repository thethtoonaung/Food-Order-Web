# Codemagic configuration file
# This file defines CI/CD workflows for your Flutter project

workflows:
  build:
    name: Build APK
    environment:
      flutter: stable

    scripts:
      - name: Get packages
        script: flutter pub get

      - name: Build APK
        script: flutter build apk --release